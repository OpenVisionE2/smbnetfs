Current restriction:
  * a lot of debug output to console.
  * truncate() and ftruncate() are not supported yet for libsmbclient < 3.2

Opened questions:
  * is it required to recode username/domain/password from local to samba encoding ?
  * Linux-2.4 and FreeBSD support status ?

